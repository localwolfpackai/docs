Snippets are reusable chunks of content that live in the `snippets/` directory. I use them to keep shared text, callouts, and components in one place so I only have to update them once. If you see the same block of content on multiple pages, it probably started here.